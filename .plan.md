# Memory issues

COULD WE DO A SINGLE REFERENCE COUNTED LANGUAGE?
i.e. the reference count only ever goes to 1 - the owner

https://www.petergirnus.com/blog/rust-vs-cc-ensuring-memory-safety-and-security
https://dev.to/vikram2784/beyond-pointers-how-rust-outshines-c-with-its-borrow-checker-1mad
https://safecpp.org/draft.html
https://manishearth.github.io/blog/2015/05/17/the-problem-with-shared-mutability/

## Buffer overflow

Slices with length
Will this impact performance by requiring a check on each access?
Require narrowing the range of a variable to within 0..length

## Use after free / dangling pointer / double free

Move semantics?
No manual free
Auto free at end of scope, once only
Don't free if var is returned -- it will be freed in parent
Return mutable props from functions -- if they may have changed
Or just pass around pointers?
Pass things around by reference if they a) don't change, or b) change but don't move
Need to free when an a var is overwritten -- e.g. var x = obj, var y = obj2, x = y, free x

## Data races

Lock type with message passing
OR, put functions that edit the data in a queue and run them one by one
Would have to prevent the data being edited otherwise (consider it moved/taken?)

## Reference cycles?

https://doc.rust-lang.org/book/ch15-06-reference-cycles.html
Weak refs? Aliases to other values?
Have some sort of sigil/holder object that you add the child objects to (so they can't point to each other and create a cycle)
e.g. parent.children: Holder<Child>[], child.parent: Holder<Child>

# Speed

https://github.com/niklas-heer/speed-comparison
https://programming-language-benchmarks.vercel.app/
https://github.com/attractivechaos/plb2

Do some advent of codes

# Interesting languages

https://github.com/rust-lang/rust
https://github.com/ziglang/zig
https://github.com/inko-lang/inko
https://github.com/odin-lang/Odin
https://graphitemaster.github.io/odin_review/
https://github.com/ValeLang/Vale
https://github.com/hylo-lang/hylo
https://github.com/roc-lang/roc
https://github.com/nim-lang/Nim
https://github.com/austral/austral/
https://github.com/onyx-lang/onyx
https://github.com/adam-mcdaniel/sage
https://github.com/BSVino/JaiPrimer/blob/master/JaiPrimer.md
https://harelang.org/
https://antelang.org/
https://www.swift.org/documentation/server/

https://www.scattered-thoughts.net/writing/assorted-thoughts-on-zig-and-rust/
https://buttondown.email/hillelwayne/archive/microfeatures-id-like-to-see-in-more-languages/
https://floooh.github.io/2018/06/17/handles-vs-pointers.html

https://github.com/vnmakarov/mir

https://www.youtube.com/watch?v=4XgIvE5SH4Y
https://developer.apple.com/videos/play/wwdc2024/10184/
https://christopher.engineering/en/blog/gleam-overview/ -- cli
https://www.youtube.com/watch?v=IulJBMbyPZg
https://old.reddit.com/r/ProgrammingLanguages/comments/1e6g6c4/nice_syntax/
https://vimeo.com/649009599

# Roadmap

- [x] pub funcs, traits, structs, declarations
- [x] `self` variable -- require self in member functions, otherwise it's static
- [n] maybe continue if x > 3, break if x > 3
  - [x] no, but break should be used to return (or `let`??)
- [ ] `give` and `take` keywords??
- [x] disposing of everything in a list
- [ ] overriding constructors
- [ ] function overloading (via `+=`??)
- [ ] multiple return
- [ ] number formats -- float, hex, octal, underscores
- [ ] `module` (or `.mod`) file containing a map (like package.json)
  - that generates a `.manifest` (or `.sym`?) file
- [x] imports
- [ ] namespaces (via folders, and maybe a top-level namespace?)
- [x] switch statements
- [ ] guard statements -- might not need them if we can be clever about if logic
- [ ] defer statements -- useful for more than just resource management
- [x] better return in ifs -- maybe no return? or `let`?
- [n] is, not operators
- [ ] pass an object after initializers to set any values you want? e.g. `const x = Y() + [ name = "hi" ]`?
- [x] nulls, null coalescing via ??
- [ ] errors, `read_file !! ""` like null coalescing?
- [x] type narrowing via if/guard e.g. after a not null check we know it's not null
- [x] match statements
- [x] enums/unions
- [x] compound assignment
- [x] functions as variables
- [ ] extending a struct e.g. with another trait
- [ ] unwind panic and todo statements
- [ ] closures!
- [ ] loop expressions, loop labels, goto etc?
- [x] generic structs
- [ ] generic functions
- [x] string interpolation
- [ ] scripting i.e. no main func
- [-] stdlib with stdout, arrays, strings etc
- [ ] prevent memory issues
- [ ] reflection
- [n] in operator e.g. if x in [0, 3] as sugar for if x == 0 || x == 3 or if x in 0..list.length
- [ ] comptime / code generation via the ast
- [ ] derive trait / can_derive_trait options with comptime
- [n] "destroy" functions? https://verdagon.dev/blog/higher-raii-uses-linear-types
- [x] do a sweep for structs, traits and functions at each scope start?
- [ ] Option: `none` for none, value otherwise, Int? shortcut
- [ ] Result: `error` for error, value otherwise, Int! shortcut
  - or Int?, !Int, !Int?
  - or Int/?, Int/!, Int/!?
  - Do we need to use `has none` and `has error`? It's a bit annoying for negatives
  - Maybe enum Result { case ok error uhoh }
- [ ] FFI, especially for system calls

Should everything be private by default and require `pub` if it's consumed outside? Or `mod`/`lib` just inside the module/library?

Do we need both switch and match? Yes, for exhaustive checking
https://erikarow.land/notes/gleam-syntax

const array = [0, 1]
const anon = [x = 5, y = 12]
const map = Map([a = "foo", b = "bar"])
const set = Set([1, 2, 3])
const list = List([1, 2, 3])

anon structs:

- change the syntax
- add anon_structs.test.ts
- implement test 080 with type coercion (e.g. Circle should have name)
  - and Math.round

https://causality.blog/essays/what-async-promised/
https://vorpus.org/blog/notes-on-structured-concurrency-or-go-statement-considered-harmful/
https://kristoff.it/blog/zig-new-async-io/

- could we do no await? async functions are just batched until variables are needed?
  or require the variable to be awaited before it is used??

/\*
func load_users = (out User[]) {}
func load_orders = (out Order[]) {}

const async = AsyncLoader()
const x = async.await(load_users)
const y = async.await(load_orders)
async.complete()
\*/

do it like generics -- when called with `await` emit async function, when called with `block` emit sync function?

or allow calling async function from sync?? `task {}` like Swift??
