# Memory issues

https://www.petergirnus.com/blog/rust-vs-cc-ensuring-memory-safety-and-security
https://dev.to/vikram2784/beyond-pointers-how-rust-outshines-c-with-its-borrow-checker-1mad
https://www.circle-lang.org/site/intro/

## Buffer overflow

Slices with length
Will this impact performance by requiring a check on each access?

## Use after free / dangling pointer / double free

Move semantics?
Auto free at end of scope, once only
Don't free if var is returned -- it will be freed in parent
Return mutable props from functions -- if they may have changed
Pass things around by reference if they a) don't change, or b) change but don't move

## Data races

Lock type with message passing
OR, put functions that edit the data in a queue and run them one by one
Would have to prevent the data being edited otherwise (consider it moved/taken?)

## Reference cycles?

https://doc.rust-lang.org/book/ch15-06-reference-cycles.html
Weak refs? Aliases to other values?
Have some sort of sigil/holder object that you add the child objects to (so they can't point to each other and create a cycle)
e.g. parent.children: Holder<Child>[], child.parent: Holder<Child>

# Speed

https://github.com/niklas-heer/speed-comparison
https://programming-language-benchmarks.vercel.app/
https://github.com/attractivechaos/plb2

Do some advent of codes

# Interesting languages

https://github.com/rust-lang/rust
https://github.com/ziglang/zig
https://github.com/inko-lang/inko
https://github.com/odin-lang/Odin
https://graphitemaster.github.io/odin_review/
https://github.com/ValeLang/Vale
https://github.com/hylo-lang/hylo
https://github.com/roc-lang/roc
https://github.com/nim-lang/Nim
https://github.com/austral/austral/
https://github.com/onyx-lang/onyx
https://github.com/adam-mcdaniel/sage
https://github.com/BSVino/JaiPrimer/blob/master/JaiPrimer.md
https://harelang.org/
https://antelang.org/
https://www.swift.org/documentation/server/

https://www.scattered-thoughts.net/writing/assorted-thoughts-on-zig-and-rust/
https://buttondown.email/hillelwayne/archive/microfeatures-id-like-to-see-in-more-languages/
https://floooh.github.io/2018/06/17/handles-vs-pointers.html

https://github.com/vnmakarov/mir

https://manishearth.github.io/blog/2015/05/17/the-problem-with-shared-mutability/
https://www.youtube.com/watch?v=4XgIvE5SH4Y
https://developer.apple.com/videos/play/wwdc2024/10184/
https://christopher.engineering/en/blog/gleam-overview/ -- cli
https://www.youtube.com/watch?v=IulJBMbyPZg
https://old.reddit.com/r/ProgrammingLanguages/comments/1e6g6c4/nice_syntax/
https://zig.guide/language-basics/payload-captures/

# Roadmap

X pub funcs, traits, structs, declarations

- `this` variable
- maybe continue if x > 3, break if x > 3
- scoping -- build up a namespace
- overriding constructors
- function overloading
- number formats -- float, hex, octal, underscores, commas
- package file with simple YAML
- imports
- namespaces (via folders, and maybe a top-level namespace?)
- switch statements
- let statements ? see below
- guard statements -- might not need them if we can be clever about if logic
- better return in ifs -- maybe no return? or 'give'?
- is, not operators
- nils, nil coalescing via ??
- type narrowing via if/guard e.g. after a not null check we know it's not null
- match statements
- enums/unions
- compound assignment
- functions as variables
- extending a struct e.g. with another trait
- unwind panic and todo statements
- scripting i.e. no main func
- stdlib with stdout, arrays, strings etc
- prevent memory issues
- reflection
- comptime / code generation via the ast
- derive trait / can_derive_trait options with comptime
- "destroy" functions? https://verdagon.dev/blog/higher-raii-uses-linear-types

Probably we want to check as we parse, putting aside any nodes with unknown types, and then doing a second pass to set their types
Or do a sweep for structs, traits and functions at each scope start?

```
if x > 5 / x is Some(y) {
  print("hi")
}
else {
  print("bye")
}

guard x > 5 / x is Some(y) else {
  return
}

when {
  case x > 10 {
    print("big x")
  }
  case x > 5 {
    print("medium x")
  }
  else {
    print("bye")
  }
}

// Do we need these? Yes, for exhaustive checking
// Do we need both of these?
// https://erikarow.land/notes/gleam-syntax

switch x {
  case > 10 && < 20 {
    print("between 10 and 20")
  }
  case 5 || 6 {
    print("5 or 6")
  }
  else {
    print("bye")
  }
}

match x {
  case Some(y) && y > 10 {
    print("big y: \(y)")
  }
  case Some(y) {
    print("small y: \(y)")
  }
  else {
    print("bye")
  }
}
```

No braces?!?
Don't require a semicolon if it's all on one line?? Or if ends with an else return?? Could be some ambiguity around if/else
We could possibly get rid of `then` if we can guarantee that lines can't start with logical operators (but also I kind of like it?)

```
// If/else if/else
if x > 5 then
  print("hi")
else if x > 10 then
  print("whoa!")
else
  print("bye")
;

// Inline if
if x > 5 then print("hi") else print("bye")
const z = if x > 5 then ~ 12 else ~ 20
const z = if x > 5 then ~ 12
          else if x > 10 ~ 20
          else ~ 80

// For
for x in 0..5 then
  print("\{x}")
;

// Inline for
for x in 0..5 then print("\{x}")
for x, i in 0..5 then print("\{i}: \{x}")

// Declarations -- const, var, func
const x: Int = 5
var y = 10
func sum: Int = (a: Int, b: Int)
  return a + b
;
func sum: Int = (a: Int, b: Int) ~ a + b

// Guard/else
guard x > 5 else
  return
;
guard x > 5 else return

// Match -- for exhaustiveness
match x
  when Some(y) && y > 10 then
    print("big y: \{y}")
  when Some(y) then
    print("small y: \{y}")
  else
    print("bye")
;
const z = match x
          when Some(y) && y > 10 then ~ 10
          when Some(y) then ~ 15
          else ~ 20
match x when true then print("true") else print("false")

// Type
type Person
  var name: String
  var address: String
  sec var ssn: String

  // Automatically generate inits at compile time?
  @comp init()

  // Custom inits
  static func init = (name: String):
    return Person.init(name, "Unknown address")
  ;

  func greet = ():
    print("Hi, \{name}")
  ;

  // Must be called before the object is dropped
  final func farewell = ():
    print("Goodbye, \{name}")
  ;
;
const andrew = Person.init("Andrew", "1 main st")

```

Should everything be private by default and require `pub` if it's consumed outside? Or `mod`/`lib` just inside the module/library?
Or how about this:

```
// If/else
if x > 5:
  print("hi")
else if x > 10:
  print("whoa!")
else:
  print("bye")
;

// Inline if
if x > 5: print("hi") else: print("bye")
const z = if x > 5 ~ 12 else ~ 20
const z = if x > 5 ~ 12
          else if x > 10 ~ 20
          else ~ 80

// For
for x in 0..5:
  print("\{x}")
;

// Inline for
for x in 0..5: print("\{x}")
for x, i in 0..5: print("\{i}: \{x}")

// Declarations -- const, var, func
const x Int = 5
var y = 10
var z String
func sum = (a Int, b Int) -> Int:
  return a + b
;
func sum = (a Int, b Int) ~ a + b
func sum = (a Int, b Int) -> Int ~ a + b
var greet (String) -> String
var act (Int, () -> Void)

// Guard/else -- for narrowing types
guard x > 5 else:
  return
;
guard x > 5 else return

// Match -- for exhaustiveness
match x
  when Some(y) && y > 10:
    print("big y: \{y}")
  when Some(y):
    print("small y: \{y}")
  else:
    print("bye")
;
const z = match x
          when Some(y) && y > 10 ~ 10
          when Some(y) ~ 15
          else ~ 20
match x when true: print("true") else: print("false")

// Checking type/enum
if x is String: etc
if x has Some(thing): etc

// Type
type Person:
  pub var name String
  pub var address String
  var ssn String

  // Automatically generate inits at compile time?
  @comp init()

  // Custom inits
  static func init = (name String):
    return Person.init(name, "Unknown address")
  ;

  pub func greet = (greeting = "Hi"):
    print("\{greeting}, \{name}")
  ;

  // Must be called before the object is dropped
  end func farewell = ():
    print("Goodbye, \{name}")
  ;
;
const andrew = Person("Andrew", "1 main st")
// or
const andrew = Person(name = "Andrew", address = "1 main st")

// Collections and types
var optional Int?
var optional = 5?
// No const optional -- that doesn't make sense!

var array Array<Int>
var array = Array<Int>()
const array = [1, 2, 3, 4]

var set Set<Int>
// etc

var map Map<String, Int>
var map = Map<String, Int>()
const map = [ "five" = 5, "six" = 6, "eight" = 8 ]

// A tuple is like an array where things can have any type,
// but also like an anon where things don't have names
var tuple Tuple<String, String>
var tuple = Tuple<String, String>()
const tuple = { "Andrew", 44 }
{ var name, var age } = tuple

// Or Dyno<A, B>?
var anon Anon<String, Int> // Not sure about this one...
var anon = { name: String, age: Int }
const anon = { name = "Andrew", age = 44 }
{ var name, var age } = anon

```

Option: `none` for none, value otherwise, Int? shortcut
Result: `error` for error, value otherwise, Int! shortcut
Do we need to use `has none` and `has error`? It's a bit annoying for negatives
