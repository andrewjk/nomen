# Console

## `struct Console`

The standard console — writing text to, and reading input from, the terminal

**Members:**

- `write(string line)`
- `write_line(string line)`
- `read_line() -> string`
- `read_char() -> char`
- `platform() -> string`
