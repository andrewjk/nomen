# Directory

## `struct Directory`

Helpers for working with filesystem directories (`create`/`remove`/`exists`)

**Members:**

- `create(string path)`
- `remove(string path)`
- `exists(string path) -> bool`
