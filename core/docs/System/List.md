# List

## `struct List<T>`

A growable, ordered list of values (`push`/`pop`/`at`)

**Members:**

- `push(T value)`
- `pop() -> T`
- `at(int i) -> T`
- `slice(int start, int end) -> view T`
- `set(int i, T value)`
